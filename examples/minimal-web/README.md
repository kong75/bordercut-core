# Minimal web example

This is an intentionally unbranded integration example for `@bordercut/core`. It demonstrates browser-side image decoding, RGBA processing, smart Remove/Keep correction strokes, and PNG export without sharing the separate product UI.

From the repository root:

```bash
npm run dev
```

Correction guides disappear when a stroke is committed because strokes are input evidence, not output pixels.
