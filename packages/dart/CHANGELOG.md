# Changelog

## 0.2.0

- Add the native algorithm v1 implementation.
- Add background and foreground correction samples.
- Add bounded, color- and edge-aware Remove and Keep strokes.
- Add byte-exact shared conformance coverage with TypeScript.
