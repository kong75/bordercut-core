name: bordercut
description: Dependency-free, deterministic background removal for typed RGBA pixel buffers.
version: 0.2.0

environment:
  sdk: ^3.6.0

resolution: workspace

dev_dependencies:
  crypto: ^3.0.6
  lints: ^6.0.0
  test: ^1.26.0
